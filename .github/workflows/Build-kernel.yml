name: Build Tab A9+ Custom Kernel (KSU)
on:
  workflow_dispatch:

jobs:
  build-kernel:
    runs-on: ubuntu-latest

    steps:
      - name: 🛠️ Prepare Environment & Install Toolchain
        run: |
          export DEBIAN_FRONTEND=noninteractive
          sudo apt-get update
          sudo apt-get install -y tzdata git software-properties-common bc build-essential flex bison libssl-dev libelf-dev clang llvm lld gcc-aarch64-linux-gnu tar zip wget python3 cpio xz-utils ccache curl

      - name: 📥 Checkout Source Code
        uses: actions/checkout@v4

      - name: 🗄️ Restore Compiler Cache
        uses: actions/cache@v4
        with:
          path: .ccache
          key: ksu-ccache-${{ github.run_id }}
          restore-keys: |
            ksu-ccache-

      - name: 📁 Setup & Permissions
        run: |
          chmod -R +x scripts/
          find . -type f -name "*.sh" -exec chmod +x {} + || true
          
          # CLEAN FIX: Ensure kernelsu is included only once to prevent double-linking
          sed -i '/kernelsu/d' drivers/Makefile
          echo "obj-y += kernelsu/" >> drivers/Makefile

      - name: 🩹 Apply Deep Source Code Fixes
        run: |
          # Architecture & Trace Fixes
          sed -i 's|register unsigned long current_stack_pointer asm ("sp");|/* Removed global sp */ #define current_stack_pointer ({ unsigned long sp; asm("mov %0, sp" : "=r"(sp)); sp; })|g' arch/arm64/include/asm/stack_pointer.h || true
          sed -i 's|"brk %0" : : "I"|"brk %0" : : "n"|g' arch/arm64/include/asm/kgdb.h || true
          ln -sf $(pwd)/arch/arm64/include/asm $(pwd)/include/asm || true
          sed -i 's|#define TRACE_INCLUDE_PATH .|#define TRACE_INCLUDE_PATH ../../kernel/sched/walt|g' kernel/sched/walt/trace.h || true
          
          # SELinux Dummy & Path Fixes
          sed -i 's/static inline u32 current_sid(void)/static inline u32 ksu_current_sid_dummy(void)/g' drivers/kernelsu/selinux/selinux.h || true
          sed -i '/extern unsigned long vdso_offset_/d' arch/arm64/include/asm/vdso.h || true
          sed -i 's|#include "ss/policydb.h"|#include "../../security/selinux/ss/policydb.h"|g' drivers/kernelsu/kernel_compat.h || true
          
          # Focaltech & App Profile Fixes
          mkdir -p drivers/input/touchscreen/focaltech_touch_spi/include/firmware
          touch drivers/input/touchscreen/focaltech_touch_spi/include/firmware/fw_sample.i || true
          sed -i 's/!!!current->seccomp.mode/0/g' drivers/kernelsu/app_profile.c || true
          sed -i 's/current->seccomp.mode = 0;/\/\/current->seccomp.mode = 0;/g' drivers/kernelsu/app_profile.c || true
          sed -i 's/current->seccomp.filter = NULL;/\/\/current->seccomp.filter = NULL;/g' drivers/kernelsu/app_profile.c || true
          
          # Fix Manual Hook path in input.c
          sed -i 's|<kernelsu/kernel/ksu.h>|"../kernelsu/kernel/ksu.h"|g' drivers/input/input.c || true
          
          # USB-C Header Path Fix
          sed -i 's|"inc/tcpci.h"|"tcpci.h"|g' drivers/usb/typec/P86801_tcpc/inc/pd_dpm_pdo_select.h || true
          
          # Fix KernelSU paths 
          sed -i 's|#include "../ksu.h"|#include "../kernel/ksu.h"|g' drivers/kernelsu/selinux/selinux.c || true
          sed -i 's|#include "../kernksu.h"|#include "../kernel/ksu.h"|g' drivers/kernelsu/selinux/selinux.c || true
          sed -i 's|#include "ksu.h"|#include "../kernel/ksu.h"|g' drivers/kernelsu/selinux/selinux.c || true
          
          # Restore rules.c SELinux struct fixes
          sed -i 's|db = \&policydb;|db = \&selinux_state.ss->policydb;|g' drivers/kernelsu/selinux/rules.c || true
          sed -i 's|selinux_status_update_policyload(0);|selinux_status_update_policyload(\&selinux_state, 0);|g' drivers/kernelsu/selinux/rules.c || true
          sed -i 's|selinux_status_update_policyload(seqno);|selinux_status_update_policyload(\&selinux_state, seqno);|g' drivers/kernelsu/selinux/rules.c || true

          # === LINKER FIXES ===
          # Remove tiny_sulog.o from build list because it's included inside supercalls.c
          sed -i 's/tiny_sulog\.o//g' drivers/kernelsu/Makefile || true
          
          # Scrub stray quoted paths that break the aarch64 linker
          sed -i 's|"drivers/kernelsu/"|drivers/kernelsu/|g' Makefile || true
          sed -i 's|"drivers/kernelsu"|drivers/kernelsu|g' Makefile || true

      - name: ⚙️ Generate Kernel Configuration
        run: |
          export ARCH=arm64
          DEFCONFIG_FILE=$(find . -name "gta9pwifi_eur_open_defconfig" | head -n 1)
          cp "$DEFCONFIG_FILE" arch/arm64/configs/custom_vendor_defconfig
          
          make ARCH=arm64 HOSTCC=gcc CC=gcc custom_vendor_defconfig
          ./scripts/config --file .config --disable SECURITY_DEFEX
          ./scripts/config --file .config --disable SEC_RESTRICT_FORK
          ./scripts/config --file .config --enable KSU
          ./scripts/config --file .config --enable KSU_NEXT
          ./scripts/config --file .config --enable KSU_NEXT_MANUAL_HOOK
          make ARCH=arm64 HOSTCC=gcc CC=gcc olddefconfig

      - name: 🔨 Resolve SELinux Headers
        run: |
          mkdir -p security/selinux/include
          mkdir -p drivers/kernelsu
          
          cat <<EOF > flask.h
          #ifndef _SELINUX_FLASK_H_
          #define _SELINUX_FLASK_H_
          #define SECCLASS_PROCESS 1
          #define SECCLASS_FILE 2
          #define SECINITSID_KERNEL 1
          #define SECINITSID_SECURITY 2
          #define SECINITSID_UNLABELED 3
          #define SECINITSID_NUM 27
          #ifndef CONFIG_SECURITY_SELINUX_SIDTAB_HASH_BITS
          #define CONFIG_SECURITY_SELINUX_SIDTAB_HASH_BITS 9
          #endif
          #endif
          EOF

          cat <<EOF > av_permissions.h
          #ifndef _SELINUX_AV_PERMISSIONS_H_
          #define _SELINUX_AV_PERMISSIONS_H_
          #define PROCESS__TRANSITION 0x00000001UL
          #define PROCESS__DYNTRANSITION 0x00000002UL
          #define PROCESS__SETFSCREATE 0x00000004UL
          #define FILE__READ 0x00000001UL
          #define FILE__WRITE 0x00000002UL
          #endif
          EOF
          
          cp flask.h security/selinux/include/flask.h
          cp av_permissions.h security/selinux/include/av_permissions.h
          cp flask.h drivers/kernelsu/flask.h
          cp av_permissions.h drivers/kernelsu/av_permissions.h

      - name: 🔨 Compile Kernel
        run: |
          export CCACHE_DIR=$GITHUB_WORKSPACE/.ccache
          ccache -M 5G
          make ARCH=arm64 \
          CC="ccache clang --target=aarch64-linux-gnu" \
          REAL_CC="ccache clang --target=aarch64-linux-gnu" \
          CROSS_COMPILE=aarch64-linux-gnu- \
          HOSTCC="ccache gcc" \
          -j$(nproc --all) \
          KCFLAGS="-Idrivers/kernelsu -Idrivers/kernelsu/kernel -Isecurity/selinux/include -Isecurity/selinux -Wno-error=frame-larger-than= -Wno-implicit-function-declaration -Wno-strict-prototypes -Wno-int-conversion -Wno-enum-conversion -Wno-implicit-int -Wno-error=implicit-int -Wno-unused-but-set-variable -Wno-error=unused-but-set-variable" \
          Image

      - name: 📦 Package Output
        run: |
          mkdir -p Final_Kernel
          cp arch/arm64/boot/Image Final_Kernel/ || true
          cd Final_Kernel
          tar -cvf TabA9Plus-KSU-Kernel.tar Image || true
          git clone https://github.com/osm0sis/AnyKernel3.git
          cd AnyKernel3
          sed -i 's/do.devicecheck=1/do.devicecheck=0/g' anykernel.sh
          sed -i 's/block=\/dev\/block\/bootdevice\/by-name\/boot;/block=auto;/g' anykernel.sh
          cp ../Image .
          zip -r9 ../TabA9Plus-KSU-AnyKernel3.zip * -x .git README.md *placeholder
          
      - name: 📤 Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: TabA9Plus-KSU-Kernel
          path: |
            Final_Kernel/TabA9Plus-KSU-Kernel.tar
            Final_Kernel/TabA9Plus-KSU-AnyKernel3.zip
